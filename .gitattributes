# Normalize line endings for all files that Git considers text files.
* text=auto eol=lf

# Only include the addons folder when creating archives (git archive / some Asset Library flows).
* export-ignore
addons -export-ignore
addons/** -export-ignore
